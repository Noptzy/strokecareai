$schema: 'https://moonrepo.dev/schemas/project.json'
project:
  type: 'application'
language: 'typescript'

fileGroups:
  sources:
    - 'src/**/*'
    - 'package.json'

dependsOn:
  - 'api'

tasks:

  test:
    command: 'vitest run'
    inputs:
      - '@globs(sources)'
